interface:
  display_name: "Squall Deep Research"
  description: "Single-agent deep research via Codex web search + optional Gemini deep research."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "squall"
      description: "Required for clink (codex/gemini), memory, memorize"
