interface:
  display_name: "Self-Improvement Loop"
  description: "Self-improvement workflow: find TODOs, improve code, curate skills using Squall memory tools."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "squall"
      description: "Required for memory, memorize"
